# 안드로이드 앱으로 만들기 (건물주 장부)

지금 웹앱을 **그대로** 안드로이드 앱으로 포장한다(Capacitor). 화면·기능은 100% 같고,
**앱을 닫아도 월말에 폰 알림이 울리는** 기능이 추가된다(서버 필요 없음).

## 왜 앱으로?
- 웹(브라우저)만으로는 "앱 꺼진 상태에서 정해진 시각에 알림"이 안 된다(웹 표준 한계).
- 안드로이드 앱은 폰이 직접 알림을 예약해서 앱이 꺼져 있어도 월말에 울려준다.

## 한 번만 준비할 것
1. **안드로이드 스튜디오**(무료) 설치 — https://developer.android.com/studio
   (자바 JDK가 같이 깔린다)
2. **구글 플레이 개발자 계정**(스토어에 올릴 때만 필요) — 1회 $25.
   본인만 쓸 거면 계정 없이 폰에 바로 설치(아래 5번)해도 된다.

## 만드는 순서 (이 폴더에서)
```bash
npm install                 # 처음 한 번 (필요한 도구 받기)
npm run android             # 웹자산 복사 → 안드로이드 프로젝트 열기(안드로이드 스튜디오)
```
`npm run android`이 하는 일:
- `www/`에 웹 화면을 모으고
- 안드로이드 프로젝트가 없으면 만든 뒤(`npx cap add android`)
- 안드로이드 스튜디오를 연다.

> 안드로이드 프로젝트(`android/`)와 `www/`는 저장소에 안 올린다(언제든 다시 만들어짐).
> 그래서 새 컴퓨터에서 처음 하면 아래를 한 번 실행해 프로젝트를 만든다:
> ```bash
> npm install
> npm run build:www
> npx cap add android
> npx cap open android
> ```

## 폰에서 실행/설치
안드로이드 스튜디오가 열리면:
1. 폰을 USB로 연결(개발자 옵션·USB 디버깅 켜기) 하거나 에뮬레이터 준비.
2. 위쪽 초록 ▶ (Run) 누르면 폰에 앱이 깔리고 실행된다.
3. 첫 실행 때 **알림 권한**을 허용해야 월말 알림이 온다.

## 웹 화면을 고친 뒤
웹 코드(js/, css/, index.html 등)를 바꿨으면 앱에도 반영:
```bash
npm run sync                # www 다시 만들고 안드로이드에 복사
```
그다음 안드로이드 스튜디오에서 다시 Run.

## 월말 알림 동작
- 매달 **25일 오전 10시**에 "은행 앱에서 거래내역 받아 정리하세요" 알림.
- 날짜는 알림 설정의 값(`bankReminderDay`, 기본 25)을 따른다.
- **알림 설정 → 월말 정리 알림**을 끄면 예약도 해제된다.
- 알림을 누르면 앱이 열리며 "은행 파일로 정리" 화면으로 간다.

## 스토어(구글 플레이)에 올리기 — 나중에
1. 안드로이드 스튜디오 → Build → Generate Signed Bundle/APK → **Android App Bundle(.aab)**.
2. 서명 키(keystore)를 만들어 안전하게 보관(잃어버리면 업데이트 못 올림).
3. 플레이 콘솔(https://play.google.com/console)에 앱 등록 후 .aab 업로드 → 심사.

## 참고
- 아이폰(iOS)은 이 방식에서 제외(안드로이드 전용).
- 오픈뱅킹 자동조회 등은 이 앱 뼈대 위에 나중에 네이티브 기능으로 붙일 수 있다.
