# DESIGN.md — 건물주 장부 디자인 시스템

> 시각 세계와 토큰의 단일 기준. CSS(`css/styles.css`)의 CSS 변수와 1:1로 일치한다.
> 모드: **Operate** (일을 끝내는 화면). 표현보다 스캔성·일관성·큰 접근성.

## 시각 세계: "든든한 장부"
따뜻한 종이 장부의 안정감 + 디지털의 또렷함. 화려한 그라디언트·유리효과·과한 그림자 금지.
신뢰를 주는 것은 장식이 아니라 **또렷한 대비, 넉넉한 여백, 큰 숫자, 일관된 색 규칙**이다.

## 타이포그래피
- 폰트: 한글 우선 시스템 스택 — `"Pretendard", -apple-system, "Apple SD Gothic Neo", "Malgun Gothic", "Noto Sans KR", system-ui, sans-serif`
- 루트 크기: 18px (고령층 기준 상향). 모든 크기는 rem.
- 스케일:
  - 본문 `--fs-body`: 1.125rem (~20px)
  - 큰 본문/버튼 `--fs-lg`: 1.25rem
  - 금액 강조 `--fs-amount`: 1.75rem, 화면 대표 숫자 `--fs-hero`: 2.5rem
  - 제목 `--fs-h1`: 1.9rem / `--fs-h2`: 1.4rem
  - 보조 `--fs-sm`: 0.95rem (최소, 남용 금지)
- 굵기: 본문 500, 강조/제목 700. 얇은 300 금지(가독성).
- 줄간격: 본문 1.6, 제목 1.25. 숫자는 `font-variant-numeric: tabular-nums`.

## 색상 (라이트, 고대비)
| 토큰 | 값 | 용도 |
|---|---|---|
| `--paper` | #F6F4EF | 앱 배경(종이) |
| `--surface` | #FFFFFF | 카드/시트 |
| `--ink` | #1A1A1A | 본문 글자 |
| `--ink-2` | #55524C | 보조 글자 |
| `--line` | #E3DFD7 | 경계선 |
| `--primary` | #1F5FA8 | 주요 동작(버튼/링크) |
| `--primary-press` | #17497F | 눌림 |
| `--on-primary` | #FFFFFF | 주요 버튼 글자 |
| `--focus` | #1F5FA8 | 포커스 링 |

### 상태색 (의미 고정 — 다른 용도로 재사용 금지)
| 상태 | 연한 배경 | 진한 글자/테두리 | 솔리드(점/막대) |
|---|---|---|---|
| 완납 (초록) | #E4F4E6 | #1E7A34 | #2E9E48 |
| 미납 (빨강) | #FBE5E3 | #B3261E | #D9382C |
| 부분/미확인 (노랑) | #FAEFCF | #8A5A00 | #E0A400 |
| 아직 확인 안 함 (회색) | #ECEAE4 | #55524C | #8A867E |

> 주의: 주요 버튼은 파랑(`--primary`)이다. 초록을 버튼색으로 쓰지 않는다(초록=완납 의미와 충돌 방지).

## 공간·형태
- 간격 스케일: 4 / 8 / 12 / 16 / 20 / 24 / 32 / 40 / 48 (px). 화면 좌우 패딩 최소 20.
- 라운드: 카드 `--r-lg` 16px, 버튼/입력 `--r-md` 12px, 칩 `--r-sm` 999px(알약).
- 그림자: `--shadow` = 0 1px 2px rgba(0,0,0,.04), 0 4px 16px rgba(0,0,0,.06). 딱 한 단계만.

## 크기 규칙 (접근성 하드 룰)
- 모든 탭 대상 최소 높이 48px, 주요 버튼 56px.
- 입금 확인 체크박스 등 핵심 조작은 큼직하게(28px 이상 아이콘/박스).
- 입력창 높이 56px, 글자 `--fs-lg`.
- 본문 대비 최소 AA(4.5:1), 큰 글자 3:1 이상. 상태는 색+글자(라벨) 병기(색맹 대응).

## 동작(모션)
- 전환 150–200ms ease-out. 화면 이동은 살짝 페이드/슬라이드.
- `prefers-reduced-motion` 존중: 모션 최소화.

## 레이아웃 패턴
- 상단: 화면 제목 + (필요시) 뒤로가기. 하단: 큰 아이콘 탭바(현황 / 세입자 / 보증금 / 더보기).
- 한 화면 한 작업. 목록은 카드 행, 각 행에 상태 점(색)+라벨(글자).
- 파괴적 동작은 하단 확인 시트로 재확인.
