# 건물주_장부 프로젝트 규칙

## 정직 원칙 (최우선, 다른 모든 규칙보다 우선)
- 나(건물주)의 지시를 따를 때 **거짓을 말하거나 사실을 왜곡하지 말 것.**
- **안 한 것을 한 것처럼 흉내 내지 말 것.** 특히 스킬·도구를 실제로 쓰지 않았으면
  "흉내만 냈다 / 스타일만 맞췄다"고 **명확히 밝힐 것.** (예: "caveman 스킬을 썼다"라고 말하려면
  실제로 그 스킬을 불러왔어야 함. 아니면 "caveman 스타일로 흉내 냈다"고 정확히 말한다.)
- **확신이 없으면 추측하지 말고 먼저 확인**한 뒤 답할 것. (파일·설치 상태 등은 실제로 확인)
- 작업 결과를 있는 그대로 보고할 것: 안 됐으면 안 됐다고, 건너뛰었으면 건너뛰었다고 말한다.

## 커뮤니케이션 스타일
- 나(건물주)에게 설명할 때: 핵심만 간결하게 축약(caveman lite 스타일 — 인사말·군더더기·과한 부연 빼고 요점 위주).
  단, 기술 용어는 쓰지 말고 쉬운 한국어로.
  ("이 부분은 데이터베이스 스키마를 바꿨어요" (X)
   "세입자 정보 저장 방식을 넓혔어요" (O))
- 내부 작업 로그·커밋 메시지: caveman 계열 스킬을 확실히 적용.
  - 커밋 메시지: caveman-commit 스킬
  - 내부 작업 로그: caveman 스킬 (lite 권장)

## 버전 업그레이드 규칙
- GitHub에 새 버전을 올릴 때마다 CHANGELOG.md에 아래 형식으로 기록
  ## [버전번호] - 날짜
  ### 추가된 기능
  ### 고친 부분
  ### 알아둘 점 (건물주가 알아야 할 변화가 있으면)
- 버전 번호는 major.minor.patch 형식 사용 (예: 1.2.0)
- 커밋 전에는 반드시 CHANGELOG.md도 같이 업데이트할 것

## 규칙 자기점검 주기
- 매 5번째 커밋마다, 이 CLAUDE.md 파일이 현재 프로젝트 상태와 맞는지 스스로 점검
- 안 맞는 규칙(예: 이미 안 쓰는 기능에 대한 규칙)이 있으면, 삭제 제안을 나에게 먼저 물어보고 반영
  (규칙을 마음대로 지우거나 바꾸지 말고, 반드시 "이 규칙 이제 필요 없어 보이는데 지울까요?"라고 먼저 물어볼 것)

---

## 앱 개요 (제작 기준 스펙)

건물주가 여러 세입자의 월세/보증금/수도세/전기세 입금 여부를 관리하는 PWA(홈 화면 설치형 웹앱).
타깃 사용자: 디지털에 익숙하지 않은 중년~노년층 건물주.

### 전체 구조
- 건물주(관리자) 1명 = 계정 1개 (한 건물에 관리자는 1명만)
- 건물주는 건물을 등록 (지금은 1개만, 구조는 여러 개 지원)
- 각 건물 아래에 세입자 여러 명 등록
- 건물주는 계좌 여러 개 등록 가능 (은행명, 계좌 별칭) — 표시/기록용, 나중에 오픈뱅킹 연동 대비

### 세입자/계약 등록 정보
- 호실 번호, 세입자 이름, 사업 이름(선택)
- 주택/상가 구분, 사업자등록번호(선택 필드, 필수 아님)
- 월세, 관리비(수도세/전기세 포함, 매달 고정 기본이나 변경 가능), 보증금, 계약 시작월
- 월세/관리비 변경 이력 관리: 변경 전 미납 건은 변경 전 금액 기준으로 표시
- ※ 세입자의 계좌번호, 주민등록번호는 절대 입력받지 않음

### 핵심 기능 — 이번 달 현황판
1. 세입자 목록에 "입금 확인" 체크박스, 호실 번호 정렬
   - 미확인: 회색 / 완납: 초록 / 미납(납기일+3일 경과): 빨강 / 부분납부: 노랑
2. 입금 확인 흐름:
   a. 건물주가 계좌에서 확인한 입금자명을 입력
   b. 등록된 세입자 이름과 느슨한 매칭으로 후보 제안
   c. 매칭 실패/애매하면 "미확인 입금"에 남기고 드롭다운으로 수동 매칭
   d. 월세+관리비 합산 입금 시 기본 관리비 포함 금액 기준 자동 인식, 차액만 표시하고 건물주가 보정
3. 완납/미납 외 "부분납부" 상태 지원 (낸 금액, 남은 금액 표시)
4. 지난 달 이력 조회, 세입자별 연체 횟수 표시

### 보증금 관리
- 입금/차감/환불을 각각 이벤트로 기록 (단순 금액이 아님)
- 차감은 항목별(수리비, 밀린 월세 등) 여러 건 등록 가능
- 계약 기간 중 수시 조회/변경 가능
- 퇴거 처리 시 최종 정산 화면(받은 보증금 - 차감 = 환불액) 자동 계산

### 계약 만료 처리
- 만료일 임박 시 알림만, 재계약/퇴거는 건물주가 직접 선택 (자동 연장 없음)

### 알림
- 미납 알림 (지금은 SMS, 나중에 카카오 알림톡 교체 대비해 발송 로직 모듈 분리)
- 알림 on/off: 전체 기본값 + 세입자별 개별 설정(덮어쓰기)

### 내보내기
- 엑셀 다운로드 + PNG 이미지 둘 다 지원
- 세입자 1명: 계약 시작일부터 전체 입금 내역
- 건물 전체: 이번 달 전체 납부 현황 한 장

### 회원가입/로그인 — 보안
- 회원가입: 휴대폰 SMS OTP 인증 (정식 PASS 본인인증은 나중에, 인증 로직 분리)
- 로그인: PWA에서 WebAuthn 지문/생체 인증 + 비상용 백업 비밀번호
- 비밀번호 해싱 저장, 모든 통신 HTTPS
- 건물주별 데이터 완전 분리

### 디자인 원칙
- 큰 글씨, 큰 버튼, 한 화면에 한 가지 작업
- 색으로 상태 구분 (초록=완료, 빨강=미납, 노랑=부분납부/미확인)
- PWA manifest로 홈 화면 아이콘 설치 가능

### 데이터 구조 (오픈뱅킹 대비)
- building, tenant, account, payment_log, deposit_ledger 테이블로 분리
- 나중에 은행 API가 payment_log에 자동으로 데이터를 넣기만 하면 되도록 설계
